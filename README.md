# PID-Motor-Controller
This code runs from an arduino ESP32 to a DC motor controller with an encoder. It uses a potentiometer which determines where the motor should land.
NOTE : The encoder was faulty during our experiment so some values are tempremental
